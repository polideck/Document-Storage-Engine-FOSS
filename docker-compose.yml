version: "2"
services:
  web:
    build: ./back-end
    ports:
      - "80:80"
    volumes:
      - ./back-end:/usr/src/app

  keycloak:
      image: wizzn/keycloak:14
      ports:
          - "8080:8080"
      environment:
          - KEYCLOAK_USER=admin
          - KEYCLOAK_PASSWORD=admin